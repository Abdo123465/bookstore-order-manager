// Configuration file to switch between local SQLite and Cloud Supabase
// Set USE_CLOUD to true to use Supabase, false to use local SQLite

export const USE_CLOUD = true; // تغييرها لـ false للرجوع لـ SQLite المحلي
